# Zeruva Backend - local development env

# Required for auth (in production this MUST be set and kept secret)
# For local dev, if omitted and NODE_ENV!=production, the server will generate a temporary secret at startup.
JWT_SECRET=change_me_to_a_long_random_string

# Allowed frontend origins (comma-separated). For local dev, localhost is allowed automatically.
# Example (later): FRONTEND_ORIGINS=https://your-frontend.vercel.app,https://staging.example.com
FRONTEND_ORIGINS=

# Solana / app config
# ADMIN_WALLET is the treasury address used to RECEIVE user payments.
# For server-side claim payouts, set DEV_WALLET_SECRET_KEY to the matching private key.
ADMIN_WALLET=
# Dev wallet secret key (server-side payer for claim payouts).
# Format: JSON array of 64 bytes (Solana keypair secretKey), or base58 of the same bytes.
DEV_WALLET_SECRET_KEY=
RPC_URL=https://api.devnet.solana.com
PUBLIC_BASE_URL=http://localhost:3000
ALIEN_COUNT=60

# Pricing
# Backend quotes SOL at purchase time using a public SOL/USD feed (CoinGecko).
# Fallback constant if the price feed is down:
USD_PER_SOL=100
# Cache TTL for SOL/USD price (ms)
PRICE_CACHE_TTL_MS=60000

# Database
DATABASE_URL=
# TLS options (recommended: leave DB_SSL_REJECT_UNAUTHORIZED=true)
DB_SSL_REJECT_UNAUTHORIZED=true
# Optional CA PEM (single-line or multiline supported depending on your env system)
DB_SSL_CA=
# Optional: disable SSL entirely
# PGSSLMODE=disable

# Used to sign spin payloads server-side (required if you rely on serverSignature)
SERVER_HMAC_SECRET=change_me_too
